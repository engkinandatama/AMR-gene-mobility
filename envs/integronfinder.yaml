name: integronfinder
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python>=3.8
  - pip
  - hmmer      # dependency IntegronFinder
  - prodigal   # dependency IntegronFinder
  - pip:
    - integronfinder
